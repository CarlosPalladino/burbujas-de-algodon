{
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^2.3.3",
    "sequelize": "^6.25.3"
  },
  "scripts": {
    "test":"node src/app.js",
    "start": "node source/app.js",
    "sq-i": "sequelize init",
    "sq-ms": "sequelize init:migrations && sequelize init:seeders",
    "sq-mg": "sequelize migration:generate --name padres_hijos",
    "sq-sg": "sequelize seed:generate --name User",
    "sq-db": "sequelize db:drop && sequelize db:create && sequelize db:migrate && sequelize "
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.5.2"
  }
}
